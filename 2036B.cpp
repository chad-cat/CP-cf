#include<iostream>
#include<vector>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n,k,g=0;
        cin>>n>>k;
        vector<int> b[i],c[i];
        for(int i=0;i<k;i++) cin>>b[i]>>c[i]; freqb[b[i]]++;
        sort(c.begin(),c.size());
         if(b.size()<n){
        for(int i=0;i<b;i++){
         m+=c[i];    
            }
        }
        else{
            for(int i=0;i<k;i++){ m+=c[i]}
        }